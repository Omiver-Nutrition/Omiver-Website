www.omiver.me {
    root * /srv/static
    file_server /static/*

    # Proxy all other requests to Gunicorn
    handle {
        reverse_proxy http://web:8000
    }
    handle /app* {
        reverse_proxy frontend:3000
    }
}
