https://dev.omiver.me {
    # Serve static files from staticfiles directory
    handle /static/* {
        root * /srv/static
        file_server
    }

    # Proxy all other requests to Gunicorn
    handle {
        reverse_proxy http://omiver_server:8000
    }
}
